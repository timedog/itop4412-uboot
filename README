itop-scp-1g is forking from u-boot-2019.01-rc3(branch master of the repository),and porting it to ITOP-4412 board.

Tips to porting (refrence doc/README.Kconfig)
-------------------------

When adding a new board, the following steps are generally needed:

 [1] Add a header file include/configs/<target>.h
      -- include/configs/itop4412.h is modification based on origen.h

 [2] Make sure to define necessary CONFIG_SYS_* in Kconfig:
       Define CONFIG_SYS_CPU="cpu" to compile arch/<arch>/cpu/<cpu>
       Define CONFIG_SYS_SOC="soc" to compile arch/<arch>/cpu/<cpu>/<soc>
       Define CONFIG_SYS_VENDOR="vendor" to compile board/<vendor>/common/*
         and board/<vendor>/<board>/*
       Define CONFIG_SYS_BOARD="board" to compile board/<board>/*
         (or board/<vendor>/<board>/* if CONFIG_SYS_VENDOR is defined)
       Define CONFIG_SYS_CONFIG_NAME="target" to include
         include/configs/<target>.h

 [3] Add a new entry to the board select menu in Kconfig.
     The board select menu is located in arch/<arch>/Kconfig or
     arch/<arch>/*/Kconfig.

 [4] Add a MAINTAINERS file
     It is generally placed at board/<board>/MAINTAINERS or
     board/<vendor>/<board>/MAINTAINERS

 [5] Add configs/<target>_defconfig
      -- Configs/itop4412_defconfig is based on ITOP-4412 official defconfig and
         origen_defconfig,as well as odroid_defconfig

 [6] Add device tree file
      -- Copy from ITOP-4412 official dts,and refrence arch/arm/dts/exynos4412-odroid.dts,
         and schematic

 [7] Add CLOCK/SDRAM initialization function
      -- Copy from ITOP-4412 official code

 [8] Enable UART/MMC device/driver
      -- Note SPL isn't initialization UART3 pins,if you want print in SPL code,you need
         config pins

 [9] Enable MMC device/driver
      -- Change states="okay" of sdhci@12530000
      -- Copy dwmmc@12550000 segment from exynos4412-odroid.dts
      -- Note:since mmc driver,we need config DOUTmmc as 100MHz,and config sclk_mmc as 50MHz

 [10] Now you can read/write SDRAM/MMC,so you can boot kernel by CONFIG_BOOTCAMMAD(defined in 
      include/configs/itop4412.h)


 How to build and download(to TF-card)
 -------------------------
 Build:
    sudo make ARCH=arm CROSS_COMPILE=/your_toolchian_path/arm-linux-gnueabihf- itop4412_defconfig
    sudo make ARCH=arm CROSS_COMPILE=/your_toolchian_path/arm-linux-gnueabihf-
    # E4412_N.bl1.SCP2G.bin provide by ITOP-4412 official
    # env.bin is a 8KB binaray file,it's all zero,because we don't need config ENV
    cat E4412_N.bl1.SCP2G.bin spl/itop4412-spl.bin env.bin u-boot.bin > itop4412-uboot.bin

 Download to /dev/your_device :
    dd iflag=dsync oflag=dsync if=itop4412-uboot.bin of=/dev/your_device seek=1
